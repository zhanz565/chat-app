# Template (commit this)
cat > server/.env.example <<'ENV'
# Port for local dev (Render provides PORT automatically in prod)
PORT=3000

# Comma-separated list of allowed web or    igins (dev + prod)
# e.g. http://localhost:5173,https://your-vercel-site.vercel.app
CLIENT_ORIGIN=http://localhost:5173
ENV

# Local dev file (DO NOT COMMIT)
cat > server/.env <<'ENV'
PORT=3000
CLIENT_ORIGIN=http://localhost:5173
ENV
